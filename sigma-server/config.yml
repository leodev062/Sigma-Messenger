database:
  host: localhost
  port: "5432"
  user: postgres
  password: senha123
  dbname: app_chat
  sslmode: disable
  timezone: UTC

auth:
  jwt_secret: "588d2bc038e24304907e55ba91e2df2c5893b7e16ed8d7d0a87222e2bbbdcb23378d10ae007443aedbd952e24205d304053b73beeb161a3860681183d0514c7e"

push:
  firebase_service_account_path: "serviceAccountKey.json"

telephony:
  provider: "mock"
  api_key: "YOUR_TELEPHONY_API_KEY"

payment:
  mercadopago_access_token: "APP_USR-3818411418220126-050910-38bdabd07819e75c75057b2f648eaebe-1191416276"

account:
  default_type: "individual"

bots:
  bot_father_username: "BotFather"
  bot_father_name: "BotFather"
  api_public_base_url: "http://localhost:3000"
  allow_insecure_webhook: true

redis:
  mode: "single"
  address: "localhost:6379"
  # Para escalar horizontalmente, troque para mode: "cluster"
  # e configure o bloco abaixo com os nós do Redis:
  # addresses:
  #   - "redis-1:6379"
  #   - "redis-2:6379"

server:
  port: "3000"
  cors:
    # Origens web permitidas (app Android nativo não usa CORS).
    # Não use "*". Em produção, liste apenas os domínios do seu front/admin.
    allow_origins:
      - "http://localhost:3000"
      - "http://127.0.0.1:3000"
  websocket:
    max_connections_per_user: 5
    # JWT via Authorization header (padrão) ou ?token= para mobile.
    # allow_token_query_param: false
    # allow_legacy_user_id_query: true  # só desenvolvimento
